// PT 1

package Array_Assignment;

public class ArrayExample {
        public static void main(String[] args) {
            // Create an array of integers with a length of 3
            int[] arr = new int[3];

            // Assign values to the array elements
            arr[0] = 1;
            arr[1] = 2;
            arr[2] = 3;

            // Print out each array element
            for (int i = 0; i < arr.length; i++) {
                System.out.println("Element at index " + i + ": " + arr[i]);
            }
        };}
 //Write a program that returns the middle element in an array.
// Give the following values to the integer array: {13, 5, 7, 68, 2} and produce the following out.

